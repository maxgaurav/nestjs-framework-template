import { NotFoundConverterInterceptor } from './not-found-converter.interceptor';

describe('NotFoundConverterInterceptor', () => {
  it('should be defined', () => {
    expect(new NotFoundConverterInterceptor()).toBeDefined();
  });
});
