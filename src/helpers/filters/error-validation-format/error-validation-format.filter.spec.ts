import { ErrorValidationFormatFilter } from './error-validation-format.filter';

describe('ErrorValidationFormatFilter', () => {
  it('should be defined', () => {
    expect(new ErrorValidationFormatFilter()).toBeDefined();
  });
});
