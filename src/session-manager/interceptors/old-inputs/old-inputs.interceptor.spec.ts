import { OldInputsInterceptor } from './old-inputs.interceptor';

describe('OldInputsInterceptor', () => {
  it('should be defined', () => {
    expect(new OldInputsInterceptor()).toBeDefined();
  });
});
