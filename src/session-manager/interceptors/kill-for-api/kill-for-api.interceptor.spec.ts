import { KillForApiInterceptor } from './kill-for-api.interceptor';

describe('KillForApiInterceptor', () => {
  it('should be defined', () => {
    expect(new KillForApiInterceptor()).toBeDefined();
  });
});
